import React from 'react';

const Timer = () => {
    
    const startingMinutes = 25;
    let time = startingMinutes * 60;
    
    return (
        <div>
            
        </div>
    );
}

export default Timer;